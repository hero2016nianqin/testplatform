# Redis 内存限制配置
# 最大内存：256MB（可根据服务器内存调整）
maxmemory 256mb

# 内存淘汰策略：当达到 maxmemory 时，淘汰最近最少使用的 key
# allkeys-lru: 所有 key 中淘汰最近最少使用的（推荐用于缓存+session场景）
maxmemory-policy allkeys-lru

# 持久化（可选，建议生产开启）
appendonly yes
appendfsync everysec
