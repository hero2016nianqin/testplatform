# 测试平台操作使用说明

## 目录

1. [系统概述](#1-系统概述)
2. [系统登录与权限](#2-系统登录与权限)
3. [工厂/产线/工站管理](#3-工厂产线工站管理)
4. [设备配置（机柜/机框/槽位）](#4-设备配置机柜机框槽位)
5. [测试参数管理](#5-测试参数管理)
6. [测试项与测试模板](#6-测试项与测试模板)
7. [测试序列编排](#7-测试序列编排)
8. [版本管理与发布流程](#8-版本管理与发布流程)
9. [软件配置与部署](#9-软件配置与部署)
10. [测试执行与监控](#10-测试执行与监控)
11. [测试记录与日志](#11-测试记录与日志)
12. [BOM 指标配置管理](#12-bom-指标配置管理)
13. [指标字典与集合管理](#13-指标字典与集合管理)
14. [脚本模板与导出](#14-脚本模板与导出)
15. [生产环境部署](#15-生产环境部署)
16. [测试服务对接指南](#16-测试服务对接指南)
17. [运维与监控](#17-运维与监控)

---

## 1. 系统概述

本平台是一套**通用制造测试管理系统**，支持多产品、多工厂、多产线的测试管理。

### 核心能力

- **设备管理**：工厂 - 产线 - 工站 - 机柜 - 机框 - 槽位 六级设备层级
- **测试版本**：测试配置的版本化管理，支持审批发布流程
- **实时监控**：WebSocket 实时推送测试状态，槽位颜色指示测试进度
- **BOM 指标**：测试指标的在线协同编辑、审批、版本快照
- **测试执行**：扫码触发测试，支持传统模式和序列模式

### 技术架构

| 组件 | 技术栈 |
|------|--------|
| 前端 | Vue 3 + TypeScript + Element Plus + Vite |
| 后端 | FastAPI + SQLAlchemy Async + Python 3.12 |
| 数据库 | PostgreSQL 15 |
| 缓存/消息 | Redis 7（Session、锁、PubSub） |
| 任务队列 | Celery + Redis Broker |
| 对象存储 | MinIO（日志归档、文件存储） |
| 容器化 | Docker Compose |

---

## 2. 系统登录与权限

### 2.1 用户角色

| 角色 | 说明 | 权限范围 |
|------|------|---------|
| `super_admin` | 超级管理员 | 全部权限，含用户管理、版本下架/恢复 |
| `developer` | 开发工程师 | 测试项/版本/BOM 的创建编辑，部署执行 |
| `process` | 工艺工程师 | 设备配置、参数管理、版本审批 |
| `operator` | 操作员 | 扫码测试、查看记录和日志 |

### 2.2 登录流程

1. 访问系统首页，自动跳转到 `/login`
2. 输入用户名和密码登录
3. 登录成功后获得 session（Redis 存储，24 小时有效期）
4. 系统根据角色显示不同的菜单和功能

### 2.3 账号注册

1. 访问 `/auth/register` 提交注册申请
2. 超级管理员在「权限管理 - 注册审批」中审核
3. 审核通过后即可登录

---

## 3. 工厂/产线/工站管理

### 3.1 设备层级结构

```
工厂 (Factory)
  └── 产线 (ProductionLine)
       └── 工站 (TestStation)
            └── 机柜 (Cabinet)
                 └── 机框 (TestChassis)
                      └── 槽位 (TestSlot)
```

### 3.2 工厂管理

- **新增工厂**：填写工厂名称、编码
- **编辑工厂**：修改工厂信息
- **删除工厂**：仅当工厂下无产线时可删除

### 3.3 产线管理

- **新增产线**：选择所属工厂，填写产线名称
- **编辑产线**：修改产线信息
- **删除产线**：仅当产线下无工站时可删除

### 3.4 工站管理

- **新增工站**：选择所属产线，填写工站名称、IP 地址
- **编辑工站**：修改工站信息
- **删除工站**：仅当工站下无设备时可删除
- **工站跳转**：点击工站进入测试监控页面

### 3.5 操作入口

- 左侧菜单 - 「线体装备」页面
- 以树形结构展示工厂 - 产线 - 工站
- 支持新增、编辑、删除操作

---

## 4. 设备配置（机柜/机框/槽位）

### 4.1 机柜 (Cabinet)

每个工站下可配置多个机柜，用于组织测试设备。

- **新增机柜**：在工站下新增，填写名称
- **机柜参数**：点击机柜右侧「参数」按钮，配置机柜级参数
  - 支持分组管理（如：电源参数、网络参数）
  - 每个参数有名称、值、分组、排序

### 4.2 机框 (TestChassis)

每个机柜下可配置多个机框。

- **新增机框**：在机柜下新增，填写名称、槽位数
- **机框参数**：点击机框右侧「参数」按钮，配置机框级参数

### 4.3 槽位 (TestSlot)

每个机框下自动根据「槽位数」创建对应数量的槽位。

**槽位状态：**

| 状态 | 颜色 | 说明 |
|------|------|------|
| `idle` | 灰色 | 空闲，可启动测试 |
| `testing` | 蓝色闪烁 | 测试进行中 |
| `pass` | 绿色 | 测试通过 |
| `fail` | 红色 | 测试失败 |
| `disabled` | 暗灰半透明 | 已禁用 |

**槽位右键菜单：**
- 开始测试：打开扫码对话框（idle/pass/fail 均可启动）
- 强制重启：非 idle 状态时可重置为 idle
- 标记通过/标记失败：手动设置状态
- 重置空闲：pass/fail 状态可重置为 idle
- 禁用/启用槽位

### 4.4 强制重启

- **单槽位重启**：右键槽位 - 强制重启
- **机框重启**：右键机框 - 强制重启（重置该机框下所有非 idle 槽位）
- **机柜重启**：右键机柜 - 强制重启（重置该机柜下所有机框的非 idle 槽位）

---

## 5. 测试参数管理

### 5.1 参数层级

| 参数类型 | 作用域 | 说明 |
|---------|--------|------|
| 硬件参数 (HardwareParam) | 工站级 | 工站的硬件配置参数 |
| 设备参数 (EquipmentConfig) | 工站级 | 工站的设备配置 |
| 场景参数 (ScenarioConfig) | 工站级 | 测试场景配置 |
| 机柜参数 (CabinetParam) | 机柜级 | 机柜维度的参数 |
| 机框参数 (ChassisParam) | 机框级 | 机框维度的参数 |

### 5.2 参数配置方式

1. 进入「测试监控」页面
2. 点击右侧「参数设置」按钮（或点击机柜/机框的「参数」链接）
3. 在参数面板中编辑参数
4. 支持参数分组、排序
5. 保存后实时生效

### 5.3 参数继承

- 版本部署时，会将版本中归档的硬件参数推送到目标工站
- 机柜/机框参数为设备级配置，不随版本变化

---

## 6. 测试项与测试模板

### 6.1 测试项 (TestItem)

传统模式下的基础测试单元，包含完整的阈值配置。

| 字段 | 说明 |
|------|------|
| 名称 | 测试项名称 |
| 期望值 | 标准测试值 |
| 最小值 | 合格下限 |
| 最大值 | 合格上限 |
| 单位 | 测量单位 |
| 分类 | 测试项分类（如：射频、功耗、性能） |
| 启用状态 | 是否启用 |

- **新增**：全局配置 - 测试项管理 - 新增
- **导入**：支持 CSV/Excel/JSON/XML 批量导入
- **导出**：支持导出为多种格式

### 6.2 测试模板 (TestItemTemplate)

序列模式下的测试单元模板，阈值在版本中配置。

| 字段 | 说明 |
|------|------|
| 名称 | 模板名称 |
| 描述 | 模板描述 |
| 服务地址 | 实际测试服务的 HTTP 接口 URL |
| 是否关键项 | 关键项失败时是否终止测试 |
| 超时时间 | 单项测试超时限制（秒） |
| 分类 | 测试项分类 |

- **模板与测试项的区别**：模板本身不含阈值，阈值在版本创建时从集合中带入
- **服务地址**：用于对接实际的测试服务（见第 16 节）

---

## 7. 测试序列编排

### 7.1 序列 (TestSequence)

将多个测试模板按顺序编排为一个完整的测试流程。

| 字段 | 说明 |
|------|------|
| 名称 | 序列名称 |
| 版本号 | 序列版本 |
| 是否启用 | 是否可用 |

### 7.2 序列步骤 (TestSequenceStep)

| 字段 | 说明 |
|------|------|
| 步骤顺序 | 执行顺序 |
| 测试模板 | 关联的 TestItemTemplate |
| 超时时间 | 该步骤的超时限制 |

### 7.3 编排流程

1. 全局配置 - 测试序列 - 新增序列
2. 为序列添加步骤，选择测试模板
3. 设置步骤顺序和超时时间
4. 保存序列

### 7.4 序列执行规则

- 按步骤顺序依次执行
- 关键项（`is_critical=True`）失败时立即终止后续步骤
- 非关键项失败时记录结果并继续执行

---

## 8. 版本管理与发布流程

### 8.1 版本类型

| 类型 | 说明 | 必填字段 |
|------|------|---------|
| `standard` | 标准版本 | 工程名称、版本号、测试序列 |
| `multi_process` | 多工序版本 | 工程名称、版本号、BOM 编码、TPS 名称、子场景 |
| `product_family` | 产品族版本 | 工程名称、版本号、BOM 编码、子场景 |

### 8.2 版本状态流转

```
draft（草稿）
  | [Stage1 审批通过]
released（已发布）
  | [Stage2 部署执行]
deployed（已部署）
  | [管理员下架]
delisted（已下架）
  | [管理员恢复]
draft（草稿）
```

### 8.3 创建版本

1. 左侧菜单 - 「版本管理」- 新建版本
2. 选择版本类型
3. 填写工程名称、版本号
4. 选择测试序列（标准版本）或配置子场景（多工序/产品族版本）
5. 上传二进制文件（可选）
6. 保存为草稿

### 8.4 审批流程

**Stage 1 - 发布审批：**

| 步骤 | 审批人 | 操作 |
|------|--------|------|
| 测试经理审核 | 指定的测试经理 | 通过/拒绝 |
| 项目经理审核 | 指定的项目经理 | 通过/拒绝 |

- 两个步骤都通过后，版本状态变为 `released`
- 任一步骤拒绝，版本保持 `draft`，可修改后重新提交

**Stage 2 - 部署审批：**

| 步骤 | 审批人 | 操作 |
|------|--------|------|
| TE 工程师审核 | 指定的 TE 工程师 | 通过/拒绝 |

### 8.5 部署执行

1. 版本状态为 `released` 后，可创建部署目标
2. 选择部署范围：工站 / 产线 / 工厂 / 全部
3. TE 工程师审批通过后，执行部署
4. 部署将版本配置推送到目标工站

### 8.6 部署推送内容

| 归档类型 | 推送目标 |
|---------|---------|
| 测试项数据 | `EquipmentMetrics.metrics_json` |
| 序列步骤 | `SoftwareConfig.sequence_data` |
| 属性页 | `EquipmentPropertyPage.page_json` |
| 硬件参数 | `HardwareParam`（替换全部） |
| 测试项集合 | `SoftwareConfig.selected_test_item_ids` |

### 8.7 版本下架与恢复

- **下架**：仅超级管理员可操作，`released` 或 `deployed` 状态可下架
- **恢复**：下架版本可恢复为 `draft`，重新走审批流程
- **删除**：仅 `draft` 或 `delisted` 状态可删除

---

## 9. 软件配置与部署

### 9.1 软件配置 (SoftwareConfig)

每个工站有一份软件配置，决定该工站执行什么测试。

| 字段 | 说明 |
|------|------|
| 项目名称 | 当前配置的项目 |
| DUT 版本 | 被测设备版本 |
| 固件版本 | DUT 固件版本 |
| 硬件版本 | DUT 硬件版本 |
| 选中的测试项 | 传统模式下选中的 TestItem ID 列表 |
| 测试序列 | 序列模式下关联的 TestSequence |
| 序列数据 | 版本部署时归档的序列快照 |
| 工序类型 | 如 FT1、OFT 等 |
| BOM 编码 | 关联的 BOM 编码 |

### 9.2 配置方式

**自动方式（推荐）：** 通过版本部署自动推送（见第 8.5 节）

**手动方式：**
1. 进入「单站配置」页面
2. 编辑软件配置
3. 选择测试项或测试序列
4. 保存

### 9.3 测试模式判断

执行测试时，系统根据以下规则判断模式：

```
if SoftwareConfig.sequence_id > 0:
    序列模式（execute_sequence_run）
else:
    传统模式（execute_test_run）
```

---

## 10. 测试执行与监控

### 10.1 进入测试监控

- 左侧菜单 - 点击某个工站 - 进入测试监控页面
- 或从「线体装备」页面点击工站

### 10.2 监控页面布局

```
+--------------------------------------------------+
|  工站名称    WebSocket 连接状态                    |
+--------------------------------------------------+
|                                                   |
|  +------+  +------+  +------+                    |
|  | 机柜1 |  | 机柜2 |  | 机柜3 |   设备区域       |
|  ||    ||  ||    ||  ||    ||                    |
|  ||机框||  ||机框||  ||机框||                    |
|  ||槽位||  ||槽位||  ||槽位||                    |
|  ||    ||  ||    ||  ||    ||                    |
|  +------+  +------+  +------+                    |
|                                                   |
+--------------------------------------------------+
|  运行日志（实时滚动）                              |
+--------------------------------------------------+
```

### 10.3 扫码测试流程

1. **选择槽位**：双击或右键空闲槽位 - 开始测试
2. **输入条码**：在弹出的扫码对话框中输入产品条码
3. **选择版本**：选择要执行的测试版本和子场景
4. **开始测试**：点击「开始测试」按钮

**系统执行流程：**

```
扫码输入
  |
校验槽位状态（仅 testing 状态拦截）
  |
获取 Redis 分布式锁
  |
创建测试批次（TestRun）
  |
根据模式分发 Celery 任务
  |- 传统模式：遍历选中的 TestItem
  |- 序列模式：按 TestSequence 步骤执行
  |
逐项执行测试（调用测试服务或模拟）
  |
实时 WebSocket 推送结果
  |
保存日志文件到磁盘
  |
释放 Redis 锁，重置槽位状态
```

### 10.4 实时状态显示

- **槽位颜色**：实时反映测试状态（idle/testing/pass/fail）
- **进度条**：testing 时显示 60%，pass/fail 时显示 100%
- **LED 指示灯**：左上角小圆点，testing 时有呼吸闪烁动画
- **日志面板**：实时滚动显示测试过程日志，支持拖拽调整大小
- **条码显示**：测试中的槽位显示当前产品条码

### 10.5 WebSocket 事件

| 事件 | 触发时机 | 数据 |
|------|---------|------|
| `run_started` | 测试开始 | slot_id, batch_id, serial_number |
| `item_tested` | 单项测试完成 | slot_id, item_name, actual_value, passed |
| `run_completed` | 全部测试完成 | slot_id, total, passed, failed |
| `run_failed` | 测试异常 | slot_id, error |

### 10.6 强制重启操作

当测试异常卡死或需要人工干预时：

1. 右键对应的槽位/机框/机柜
2. 选择「强制重启」
3. 系统重置所有非 idle 槽位为 idle 状态
4. 释放 Redis 分布式锁
5. 日志面板显示重置的槽位信息

---

## 11. 测试记录与日志

### 11.1 测试记录查询

- 左侧菜单 - 「测试记录」
- 支持按工站、时间范围、状态（全部/通过/失败）筛选
- 分页展示测试批次列表
- 每条记录显示：批次号、条码、工站、状态、开始/结束时间、通过率

### 11.2 测试详情

- 点击测试记录查看详细结果
- 展示每项测试的：测试项名称、期望值、实测值、偏差、结果（PASS/FAIL）、耗时
- 支持导出为 CSV/Excel

### 11.3 运行日志

- 左侧菜单 - 「日志查询」
- 支持按工站、时间范围、级别筛选
- 日志按 `{工站ID}/success|fail/{条码}_{时间}.log` 结构存储
- 30 天以上日志自动压缩归档至 MinIO

---

## 12. BOM 指标配置管理

### 12.1 BOM 配置概述

BOM（Bill of Materials）配置用于管理产品测试指标参数，支持版本化管理和协同编辑。

**核心概念：**
- **BOM 编码**：产品的唯一标识，一个 BOM 编码下可有多个版本
- **版本**：BOM 配置的快照，支持草稿/审批/发布/归档状态
- **指标**：BOM 下的具体测试参数项
- **领域责任人**：按测试领域分配负责人

### 12.2 BOM 配置操作

- 左侧菜单 - 「BOM 指标配置」
- **新建 BOM**：填写 BOM 编码、名称、版本号
- **查看列表**：按 BOM 编码分组，显示最新版本
- **复制 BOM**：从已有 BOM 复制创建新配置

### 12.3 BOM 详情编辑

进入 BOM 详情页面后：

**指标管理：**
- 新增指标：选择指标字典中的指标，设置参数值
- 批量添加：从指标集合中批量导入
- 在线编辑：直接在表格中修改参数值
- 删除指标：移除不需要的指标

**参数编辑：**
- 每个指标下可配置多个参数
- 参数支持：名称、值、单位、类型
- 支持新增、编辑、删除参数

**协同编辑：**
- 多人可同时编辑同一个 BOM
- 实时显示谁在编辑哪个指标（WebSocket 推送）
- 编辑锁机制：同一时间只允许一人编辑同一指标
- 断线重连后自动同步最新编辑状态

### 12.4 BOM 审批流程

```
draft（草稿）
  | [提交审批]
submitted（已提交）
  | [审批通过]
approved（已通过）
  | [归档发布]
archived（已归档）
  | [新建迭代]
draft（新版本草稿）
```

**审批操作：**
- 提交审批：开发者提交 BOM 进入审批流程
- 审批通过：审批人确认通过
- 审批拒绝：审批人拒绝，返回修改
- 撤回：提交者可撤回已提交的审批
- 归档：审批通过后归档发布

### 12.5 BOM 导入导出

- **导出 Excel**：导出 BOM 配置为 Excel 文件
- **导出 PDF**：导出为 PDF 文档
- **导出差异报告**：对比不同版本的差异
- **导入**：从 Excel 导入 BOM 配置（支持 dry_run 预览）

### 12.6 版本对比与回滚

- 查看版本历史记录
- 对比任意两个版本的差异
- 一键回滚到历史版本

---

## 13. 指标字典与集合管理

### 13.1 指标字典 (IndicatorDict)

全局统一的指标定义库，所有 BOM 配置的指标都来源于此。

**字段说明：**

| 字段 | 说明 |
|------|------|
| 指标编码 | 唯一标识 |
| 指标名称 | 中文名称 |
| 单位 | 测量单位 |
| 值类型 | 数值/文本/布尔 |
| 默认值 | 默认参数值 |
| 脚本模板 | 关联的 Python 脚本 |

**操作：**
- 左侧菜单 - 「指标字典库」
- 新增、编辑、删除指标
- 批量导入/导出

### 13.2 测试项集合 (TestItemCollection)

将多个指标组合为一个集合，方便 BOM 配置时批量引用。

**操作：**
- 左侧菜单 - 「测试项集合管理」
- 新建集合，添加指标
- 设置每个指标的默认参数
- 集合可被多个 BOM 配置引用

### 13.3 指标版本记录

- 自动记录每次指标变更
- 支持查看历史版本和变更内容
- 支持版本对比和回滚

---

## 14. 脚本模板与导出

### 14.1 脚本模板 (ScriptTemplate)

允许用户编写 Python 脚本来处理和转换指标数据。

**字段说明：**

| 字段 | 说明 |
|------|------|
| 名称 | 脚本名称（唯一） |
| 源代码 | Python 代码 |
| 输出格式 | JSON 或 INI |
| 启用状态 | 是否可用 |

**脚本执行环境：**
- 提供 `indicator_data` 变量（指标数据列表）
- 可用模块：`json`、`configparser`
- 脚本需定义 `result` 变量作为输出
- 沙箱执行，无文件/网络/系统访问权限

### 14.2 脚本执行

1. 左侧菜单 - 「自定义脚本模板」
2. 选择要执行的脚本
3. 选择目标指标/集合/BOM
4. 执行脚本
5. 下载生成的 JSON/INI 文件

### 14.3 指标查询导出

- 左侧菜单 - 「指标查询导出」
- 跨表联合查询：BOM + 集合 + 指标
- 筛选条件：BOM 编码、指标名称、状态等
- 导出为 Excel

---

## 15. 生产环境部署

### 15.1 环境要求

| 组件 | 最低配置 |
|------|---------|
| CPU | 4 核 |
| 内存 | 8 GB |
| 磁盘 | 50 GB SSD |
| Docker | 20.10+ |
| Docker Compose | 2.0+ |

### 15.2 部署步骤

```bash
# 1. 克隆代码
git clone git@github.com:hero2016nianqin/testplatform.git
cd testplatform

# 2. 配置环境变量
cd deploy
cp .env.example .env
# 编辑 .env，修改以下关键配置：
#   - SECRET_KEY：改为随机强密码
#   - DB_PASSWORD：改为强密码
#   - REDIS_URL：确认 Redis 地址
#   - MINIO_*：确认 MinIO 配置

# 3. 启动所有服务
docker compose up -d

# 4. 查看服务状态
docker compose ps

# 5. 查看日志
docker compose logs -f backend
```

### 15.3 服务清单

| 服务 | 端口 | 说明 |
|------|------|------|
| frontend | 80 | Nginx 前端 + API 反向代理 |
| backend | 8000 (内部) | FastAPI 后端 |
| celery-worker | - | Celery 测试执行 worker |
| celery-beat | - | Celery 定时任务调度 |
| postgres | 5432 (内部) | PostgreSQL 数据库 |
| redis | 6379 (内部) | Redis 缓存/消息 |
| minio | 9000/9001 (内部) | MinIO 对象存储 |
| pg-backup | - | 数据库自动备份 |

### 15.4 首次部署

1. 启动服务后访问 `http://服务器IP`
2. 进入系统初始化页面，创建初始数据
3. 创建超级管理员账号
4. 配置工厂、产线、工站
5. 创建测试项和测试序列
6. 创建测试版本并部署

### 15.5 日常运维

```bash
# 查看服务状态
docker compose ps

# 重启单个服务
docker compose restart backend

# 查看后端日志
docker compose logs -f backend

# 查看 Celery worker 日志
docker compose logs -f celery-worker

# 停止所有服务
docker compose down

# 更新代码后重新部署
git pull
docker compose up -d --build
```

---

## 16. 测试服务对接指南

### 16.1 当前状态

当前测试执行使用**模拟数据**（随机生成 90% 通过率），未对接真实测试服务。

### 16.2 对接方式

要对接真实测试服务，需要修改以下文件：

**方式一：HTTP 接口调用**

在 `backend/tasks/test_tasks.py` 中，将随机模拟替换为 HTTP 调用：

```python
import httpx

async def call_test_service(service_url: str, params: dict) -> dict:
    """调用实际测试服务"""
    async with httpx.AsyncClient(timeout=30) as client:
        response = await client.post(service_url, json=params)
        return response.json()

# 在 execute_test_run / execute_sequence_run 中调用：
result = await call_test_service(
    item.service_address,  # TestItemTemplate.service_address
    {
        "serial_number": serial_number,
        "test_item_name": item.name,
        "expected_value": item.expected_value,
        "min_value": item.min_value,
        "max_value": item.max_value,
    }
)
actual_value = result.get("actual_value")
passed = result.get("passed", False)
```

**方式二：Celery 任务调用**

在 `backend/tasks/test_tasks.py` 中，调用产品团队的 Celery 任务：

```python
from celery import chord

# 将测试项分发到产品团队的 worker
result = chord(
    [execute_product_test.s(item_id, serial_number) for item_id in item_ids],
    on_all_complete.s(run_id)
).apply_async()
```

### 16.3 测试服务接口规范

建议产品团队提供的测试服务接口遵循以下规范：

**请求格式：**
```json
POST /api/v1/test/execute
{
    "serial_number": "产品条码",
    "test_item_name": "测试项名称",
    "expected_value": 3.3,
    "min_value": 3.0,
    "max_value": 3.6,
    "unit": "V",
    "timeout_seconds": 30
}
```

**响应格式：**
```json
{
    "actual_value": 3.35,
    "passed": true,
    "duration_ms": 1500,
    "message": ""
}
```

### 16.4 各产品团队的职责

| 职责 | 说明 |
|------|------|
| 定义测试模板 | 在平台中创建 TestItemTemplate，填写服务地址 |
| 实现测试服务 | 开发 HTTP 接口，接收测试参数，返回结果 |
| 配置测试序列 | 编排测试步骤，设置超时和关键项 |
| 创建版本 | 打包测试配置，提交审批 |
| 部署到工站 | 执行版本部署，推送配置到目标工站 |

---

## 17. 运维与监控

### 17.1 健康检查

**后端健康检查：**
```bash
curl http://localhost:8000/api/v1/health
```

**Redis 连接检查：**
```bash
docker compose exec redis redis-cli ping
```

**PostgreSQL 连接检查：**
```bash
docker compose exec postgres pg_isready -U testplatform
```

### 17.2 监控指标

| 指标 | 说明 | 告警阈值 |
|------|------|---------|
| API 响应时间 | 平均响应时间 | > 2s |
| 活跃 WebSocket 连接数 | 当前连接数 | > 1000 |
| Celery 队列长度 | 待处理任务数 | > 50 |
| Redis 内存使用率 | 当前使用/maxmemory | > 80% |
| PostgreSQL 连接数 | 活跃连接数 | > 80% |
| 磁盘使用率 | 数据盘使用率 | > 85% |

### 17.3 日志管理

**应用日志：**
- 路径：`logs/app.log`
- 级别：生产环境建议 INFO
- 轮转：Docker json-file 驱动，10MB/文件，保留 3 个

**测试日志：**
- 路径：`logs/{工站ID}/success|fail/{条码}_{时间}.log`
- 归档：30 天以上自动压缩至 MinIO
- 保留：默认保留 90 天

**数据库备份：**
- 自动备份：每天凌晨 2 点
- 保留策略：默认 30 天
- 备份位置：`/backups/` 目录
- 备份格式：gzip 压缩的 SQL 文件

### 17.4 故障排查

**WebSocket 连接失败：**
1. 检查 Nginx 配置中的 WebSocket 代理
2. 检查 Redis PubSub 是否正常
3. 查看后端日志中的连接错误

**测试任务卡死：**
1. 检查 Celery worker 是否运行：`docker compose ps celery-worker`
2. 检查 Redis 分布式锁是否过期
3. 使用「强制重启」功能重置槽位

**数据库连接耗尽：**
1. 检查 `DB_POOL_SIZE` 和 `DB_MAX_OVERFLOW` 配置
2. 检查是否有慢查询
3. 考虑增加连接池大小

### 17.5 安全建议

1. **修改默认密码**：部署前必须修改 SECRET_KEY 和 DB_PASSWORD
2. **启用 HTTPS**：配置 SSL 证书，Nginx 监听 443 端口
3. **限制访问**：配置防火墙，只开放必要端口
4. **定期更新**：及时更新 Docker 镜像和依赖
5. **备份验证**：定期验证数据库备份可恢复

---

## 附录 A：API 端点速查

### 工站管理
- `GET /api/v1/stations` - 工站列表
- `POST /api/v1/stations` - 创建工站
- `GET /api/v1/stations/{id}/detail` - 工站详情
- `PUT /api/v1/stations/{id}` - 更新工站
- `DELETE /api/v1/stations/{id}` - 删除工站

### 设备配置
- `GET /api/v1/stations/{id}/cabinets` - 机柜列表
- `POST /api/v1/stations/{id}/cabinets` - 创建机柜
- `POST /api/v1/stations/cabinets/{id}/chassis` - 创建机框
- `GET /api/v1/stations/{id}/params` - 参数列表
- `PUT /api/v1/stations/{id}/params` - 更新参数

### 测试执行
- `POST /api/v1/tests/scan` - 扫码测试
- `POST /api/v1/tests/runs` - 创建测试批次
- `GET /api/v1/tests/runs` - 测试记录列表
- `GET /api/v1/tests/runs/{id}` - 测试详情

### 版本管理
- `GET /api/v1/versions` - 版本列表
- `POST /api/v1/versions` - 创建版本
- `PUT /api/v1/versions/{id}` - 更新版本
- `POST /api/v1/versions/{id}/submit` - 提交审批
- `POST /api/v1/versions/{id}/approve` - 审批通过
- `POST /api/v1/versions/{id}/deploy` - 执行部署

### BOM 管理
- `GET /api/v1/metrics/bom-configs` - BOM 列表
- `POST /api/v1/metrics/bom-configs` - 创建 BOM
- `PUT /api/v1/metrics/bom-configs/{id}` - 更新 BOM
- `POST /api/v1/metrics/bom-configs/{id}/submit-review` - 提交审批

### 强制重启
- `POST /api/v1/stations/slots/{id}/restart` - 重启单槽位
- `POST /api/v1/stations/chassis/{id}/restart` - 重启机框
- `POST /api/v1/stations/cabinets/{id}/restart` - 重启机柜

---

## 附录 B：常见问题

**Q: 槽位卡在 testing 状态怎么办？**
A: 右键槽位 - 强制重启，或右键机框/机柜批量重启。

**Q: 如何查看测试日志？**
A: 左侧菜单 - 日志查询，按工站和时间筛选；或在测试监控页面的日志面板查看实时日志。

**Q: 版本部署后没有生效？**
A: 检查版本状态是否为 deployed；检查目标工站的 SoftwareConfig 是否已更新；尝试刷新页面。

**Q: 如何对接自己的测试服务？**
A: 参见第 16 节「测试服务对接指南」，在 TestItemTemplate 中配置服务地址，修改 Celery 任务调用真实接口。

**Q: Redis 内存满了怎么办？**
A: 系统已配置 maxmemory=256mb + allkeys-lru 淘汰策略，会自动淘汰过期数据。如需更多内存，修改 `deploy/redis.conf` 中的 maxmemory 值。
